{
  "version": 2,
  "buildCommand": "bash -c 'if [ -n \"$API_URL\" ]; then sed -i \"s|window.__API_URL__|window.__API_URL__ = \\\"$API_URL\\\";|g\" index.html 2>/dev/null || true; fi'",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        }
      ]
    }
  ]
}

