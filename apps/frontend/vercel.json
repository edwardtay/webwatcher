{
  "version": 2,
  "buildCommand": "mkdir -p dist && cp index.html dist/ && cp -r js dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "installCommand": "echo 'No dependencies to install'"
}

